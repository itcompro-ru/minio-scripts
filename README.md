# minio-scripts
Utility scripts for [MinIO](https://min.io/) storage

[MinIO client](https://github.com/minio/mc) is required to run scripts.

1. install.sh - download and install minio as systemd service (tested on Ubuntu 18.04)
2. newuser.sh - creates new user with single bucket access
